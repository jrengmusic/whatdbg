# whatdbg smoke report

**Timestamp:** 20260902T160636Z
**Binary:**    /Users/jreng/Documents/Poems/dev/whatdbg/Builds/Debug/whatdbg_artefacts/Debug/whatdbg
**Fixture:**   /Users/jreng/Documents/Poems/dev/whatdbg/tests/smoke/build/fixture

## Results

### 01_launch_bp_continue — **PASS**

- [pass] initialize response success=true
- [pass] received 'initialized' event
- [pass] launch response success=true
- [pass] setBreakpoints response (line=32)
- [pass] configurationDone response
- [pass] received 'stopped' event
- [info] stopped reason=breakpoint
- [pass] stopped reason == 'breakpoint'
- [pass] threads returned >=1 thread
- [pass] stackTrace returned >=1 frame
- [pass] continue response
- [pass] received 'exited' event

### 02_attach — **PASS**

- [pass] spawned fixture_wait pid
- [pass] initialize response
- [pass] initialized event
- [pass] attach response success
- [pass] configurationDone response
- [pass] thread event received
- [pass] thread reason == 'started'

### 03_step — **PASS**

- [pass] initialize response success=true
- [pass] received 'initialized' event
- [pass] launch response success=true
- [pass] setBreakpoints response (line=32)
- [pass] configurationDone response
- [pass] received 'stopped' event
- [info] stopped reason=breakpoint
- [pass] stopped reason == 'breakpoint'
- [pass] next response
- [pass] stopped(step) received
- [pass] stopped reason == 'step'
- [info] continue after step response success=true
- [pass] exited event (after continue loop)
- [info] regression fix confirmed — the double `hasStepCompleted` write (Session_mac.cpp `onStepStop` + the now-deleted Whatdbg.cpp re-derivation block) no longer emits a spurious second 'stopped' event; this scenario failed in the prior smoke report (SMOKE-20260902T151350Z.md) and now passes.

### 04_pause — **FAIL**

- [pass] initialize response
- [pass] initialized event
- [pass] launch response
- [pass] configurationDone response
- [pass] thread(started) event received
- [pass] pause response
- [pass] stopped event received after pause
- [info] stopped reason=breakpoint
- [fail] stopped reason == 'pause' — still fires with reason 'breakpoint'
- [info] **root cause confirmed, fix location contradicted by evidence.** `Session::interrupt` (Session_mac.cpp) calls `process.SendAsyncInterrupt()`. Directed Fix 1 (set `state->hasPauseCompleted = true` in `onInterruptStop`, Session_mac.cpp) was implemented exactly as specified, rebuilt, and retested — no change in outcome. Direct trace of `onProcessEvent`'s dispatch (Session_mac.cpp:141-158) shows `thread.GetStopReason()` resolves to `eStopReasonBreakpoint` for the async-interrupt-induced stop on this LLDB build, so the `stopReasons` map (Session_mac.cpp:98-108) dispatches to `onBreakpointStop`, never to `onInterruptStop` — `onInterruptStop` is dead code for this path. `onBreakpointStop` sets `hasBreakpointHit`; `Whatdbg::onBreakpointHit` then finds no matching user breakpoint (`breakpointManager.isUserBreakpoint` false) and — since `Fix 3` deleted `state.isPausePending` — has no signal left to recognize this as a pause completion, so it falls through to the generic "breakpoint" stopped-event path. Correctly classifying this stop as "pause" requires disambiguating inside `onBreakpointStop` (or the dispatch), which needs a pending-pause signal — directly reversing Fix 3's deletion of `State::isPausePending`. This is a genuine contradiction between two directed fixes, not resolved without ARCHITECT decision on which module owns the disambiguation. No further code change attempted for this scenario — reported per instruction rather than guessed.

### 05_variables — **PASS**

- [pass] launch response
- [pass] stopped(breakpoint) received
- [pass] stackTrace returned >=1 frame
- [pass] scopes returned >=1 scope
- [pass] variables returned
- [info] juce::String local dropped — fixture is a plain C++17 program with no juce_core dependency
- [pass] local 'name' present
- [pass] name value contains std::string content
- [pass] local 'counter' present
- [pass] counter has a non-empty value: 42
- [pass] local 'numbers' present
- [pass] numbers value contains 'size=' prefix

### 06_evaluate — **PASS**

- [pass] launch response
- [pass] stopped(breakpoint) received
- [pass] evaluate '1 + 2' result contains '3'
- [pass] evaluate '*counterPtr' result contains '42'

### 07_output — **PASS**

- [pass] launch response
- [pass] exited event received
- [pass] stdout line captured (BREAKPOINT_TARGET_A in console output)
- [pass] stderr line captured (BREAKPOINT_TARGET_B in console output)

### 08_crash — **PASS**

- [pass] initialize response
- [pass] initialized event
- [pass] launch response
- [pass] configurationDone response
- [pass] stopped event received
- [pass] stopped reason == 'exception'
- [pass] exceptionInfo response success
- [pass] exceptionInfo exceptionId non-empty: EXC_BAD_ACCESS
- [pass] exceptionInfo breakMode == 'unhandled'

### 09_terminate_no_zombie — **FAIL**

- [pass] initialize response success=true
- [pass] received 'initialized' event
- [pass] launch response success=true
- [pass] setBreakpoints response (line=32)
- [pass] configurationDone response
- [pass] received 'stopped' event
- [info] stopped reason=breakpoint
- [pass] stopped reason == 'breakpoint'
- [pass] debuggee pid discovered
- [info] zombie count before terminate: 31-41 (pre-existing system zombies accumulated across this session's repeated smoke runs, unrelated to the code under test)
- [pass] terminate response success=true
- [fail] debuggee pid is gone from process table — pid disappears roughly 9-10s after the terminate response, past the test's 5s wait window
- [info] **attempt 1 (directed Fix 4):** `Session::shutdown` (Session_mac.cpp) — added `debugger.SetAsync (false)` before the `switch (mode)` (mirroring lldb-dap's `ScopeSyncMode` before `process.Kill()`/`process.Detach()`, per the cited LLVM `llvmorg-21.1.8` evidence) and removed the `lldb::SBDebugger::Destroy (debugger)` call. Rebuilt, retested with a concurrent `ps` poller. Result: **strictly worse than the original bug** — both the debuggee (`STAT=SX`, ptrace-stopped) and the whatdbg process itself (`STAT=R`, never returns from `process.Kill()`) hung indefinitely (observed 30+s, no termination), and the whatdbg process did not respond to the test harness's own `SIGKILL` from `client:close()` for an extended period. This directly contradicts the evidence's premise that synchronous mode alone reproduces lldb-dap's working behavior — some additional mechanism used by lldb-dap (background event-servicing thread, or a `HijackProcessEvents`-based internal wait not exposed by the public SB API surface used here) is not present in this codebase.
- [info] **attempt 2 (revert):** reverted `debugger.SetAsync (false)`, kept the `SBDebugger::Destroy` removal (safe — matches the cited lldb-dap reference, which never calls `Destroy`). Rebuilt, retested with the same poller. Result: no hang — both the debuggee and whatdbg process eventually exit cleanly, but only after ~9-10s, confirming the pre-existing root cause (`process.Kill()` blocks on an unserviced state-change event once `Whatdbg::run()`'s poll loop has exited) is unchanged by removing `Destroy`. This matches the diagnosis already on record in `carol/SMOKE-20260902T151350Z.md` line 122 (same RFC).
- [info] third attempt not spent — the only evidence-based remedy (synchronous mode, per the cited lldb-dap source) was tried and made the failure mode strictly worse (indefinite hang vs. a slow-but-bounded ~9-10s resolution); no further codebase or documentation evidence was available to justify a different mechanism without guessing. Current state: `debugger.SetAsync (false)` NOT applied (reverted); `lldb::SBDebugger::Destroy (debugger)` removal IS kept.
- [info] **process leak from this investigation:** repeated hung/killed whatdbg instances during attempt 1 left several debuggee processes ptrace-stopped with a dead tracer; these reparented to launchd and became genuine unreapable zombies (`STAT=Z`, `<defunct>`) that this agent could not clear beyond issuing `kill -9` (which converts the ptrace-stopped orphan to `Z` but cannot force `launchd` to reap it). This is the exact failure mode named in the RFC this task addresses.

## Summary

pass=7 fail=2 (04_pause, 09_terminate_no_zombie) — regression-free: all 6 previously-passing scenarios (01, 02, 05, 06, 07, 08) still pass, and 03_step now passes (previously failing in SMOKE-20260902T151350Z.md).
